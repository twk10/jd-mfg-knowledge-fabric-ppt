window.SLIDES = window.SLIDES || [];
window.SLIDES.push({
  id: 'why-win',
  kicker: 'Act 2 \u00b7 Better than a Google search',
  html:
    '<h1 class="title">Why the agent wins</h1>' +
    '<p class="sub">Not by knowing more than the internet \u2014 by knowing <i>our</i> corner of it perfectly.</p>' +
    '<div class="grid c2" style="margin-top:30px">' +
    '<div class="card ac-jade frag"><span class="ic">' + icon('shield') + '</span>' +
    '<h3>Grounded in our documentation</h3><p>Answers reflect the exact versions, conventions and constraints we run in production. When the docs change, the answers change with them.</p></div>' +
    '<div class="card ac-turmeric frag"><span class="ic">' + icon('list') + '</span>' +
    '<h3>Complete enumerations, guaranteed</h3><p>Lists and state tables are parsed deterministically into the knowledge graph. An LLM samples; a parser doesn\u2019t. 8/8 events, all 6 exit states \u2014 every time.</p></div>' +
    '<div class="card ac-sapphire frag"><span class="ic">' + icon('quote') + '</span>' +
    '<h3>Auditable citations</h3><p>Every claim carries numbered sources down to file, section and line. Google gives you links; we give you evidence.</p></div>' +
    '<div class="card ac-flamingo frag"><span class="ic">' + icon('lock') + '</span>' +
    '<h3>Fully private, fully reproducible</h3><p>Local models, local stores, deterministic ids \u2014 the same corpus rebuilds byte-identical knowledge. Nothing leaves the machine.</p></div>' +
    '</div>'
});
